# Server-only email configuration. Never prefix these with NEXT_PUBLIC_.
EMAIL_SERVICE=Gmail
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_TO=
# Optional verified sender address; defaults to EMAIL_USER.
EMAIL_FROM=

# Optional public origin for canonical and social-sharing URLs (e.g. https://your-domain.com).
SITE_URL=
